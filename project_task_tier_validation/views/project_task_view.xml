<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2025 ForgeFlow S.L.
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl). -->
<odoo>
    <record id="view_project_task_filter" model="ir.ui.view">
        <field name="name">project.task.select - project_task_tier_validation</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_search_form_base" />
        <field name="arch" type="xml">
            <filter name="starred_tasks" position="after">
                <separator />
                <filter
                    name="needs_review"
                    string="Needs my Review"
                    domain="[('reviewer_ids','in',uid), ('state', 'not in', ['done', 'cancel','to approve'])]"
                    help="My Purchases to review"
                />
                <filter
                    name="tier_validated"
                    string="Validated"
                    domain="[('validated', '=', True)]"
                    help="POs validated and ready to be confirmed"
                />
            </filter>
        </field>
    </record>
    <record id="project_task_kpis_tree" model="ir.ui.view">
        <field name="name">project.task.inherit.project.task.list</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.project_task_view_tree_main_base" />
        <field name="arch" type="xml">
            <list position="inside">
                <field
                    name="validation_status"
                    widget="badge"
                    optional="hide"
                    decoration-info="validation_status == 'pending'"
                    decoration-danger="validation_status == 'rejected'"
                    decoration-success="validation_status == 'validated'"
                />
            </list>
        </field>
    </record>
</odoo>
